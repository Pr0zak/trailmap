package com.trailmap.ui.theme

import androidx.compose.material3.Typography

// Default Material3 type scale; kept in its own file so screens can import a stable name.
val TrailmapTypography = Typography()
